id: websocket
config:
  url: 'ws://127.0.0.1:8080/ws/chat'
  messageTemplate: |
    {
      "message": "{{query}}",
      "chatSessionId": "{{sessionId}}"
    }
  transformResponse: 'data.message'
  timeoutMs: 30000
  keepAlive: true

