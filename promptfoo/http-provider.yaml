id: http
config:
  useHttps: false
  url: 'http://localhost:8080/api/chat'
  method: POST
  headers:
      x-model: '{{model}}' # A way to control offline test execution. Disable on prod
  body:
      message: '{{query}}'
      sessionId: '{{sessionId}}'

  transformResponse: 'json.message'
