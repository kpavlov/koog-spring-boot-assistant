id: http
config:
  useHttps: false
  request: |
    POST /api/chat HTTP/1.1
    Host: localhost:8080
    Content-Type: application/json

    {
      "message": "{{query}}",
      "sessionId": "{{sessionId}}"
    }
